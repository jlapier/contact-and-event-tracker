<%= form.input :role, :as => :select, :collection => roles_for_select, :include_blank => false %>
<%= form.input :email, :hint => 'you must be able to check mail for this address' %>
<%= form.input :password, :hint => form.object.new_record? ? nil : "Change password" %>
<%= form.input :password_confirmation %>